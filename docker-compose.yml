services:
  app:
    build: .
    ports:
      - "5001:5000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./system_prompt.txt:/app/system_prompt.txt:ro
    restart: unless-stopped
